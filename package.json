{
  "name": "to-do-list-application",
  "version": "1.0.0",
  "description": "- Criar uma funcionalidade de TO-DO list, uma API com NodeJS e React na interface. Não é necessário autenticação e proteção por usuário, será apenas um teste. Qualquer banco de dados pode ser utilizado.",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon",
    "migrate": "npx knex migrate:rollback && npx knex migrate:latest && npx knex seed:run",
    "start": "node server.js",
    "start:prod": "NODE_ENV=prod node server.js"
  },
  "author": "Jefferson Coelho",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv-flow": "^3.2.0",
    "express": "^4.17.1",
    "knex": "^0.21.16",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
